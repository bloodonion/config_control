---
- name: Replace line in file
  hosts: all
  become: true
  tasks:
    - name: editing line in defaults
      lineinfile:
        path: /etc/default/test_file
        line: "{{ edit1 }}"
        state: present
        insertafter: 2
        regexp: '^.*$'
